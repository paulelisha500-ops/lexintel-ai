# Installed in its own Docker layer AFTER requirements.txt so adding a
# package here doesn't invalidate the multi-GB torch/spaCy layer above it.

# Local speech-to-text for the courtroom stand and audio evidence. Runs on
# CPU; courtroom audio never leaves the machine.
faster-whisper==1.0.3

# Small English NER model (~12 MB, ~50 MB RAM). The transformer model
# (en_core_web_trf) is still used if SPACY_MODEL=en_core_web_trf, but it needs
# far more memory than a laptop running several projects can spare.
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
